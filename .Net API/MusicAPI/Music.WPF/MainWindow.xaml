<Window x:Class="Music.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Name="mainWindow">
    <DockPanel Name="MainPanel">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File" HorizontalAlignment="Right">
                <MenuItem Header="Load" Click="Load_Click" HorizontalAlignment="Right" />
            </MenuItem>
        </Menu>
        <!--<DataGrid ItemsSource="{Binding Brani}"  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Titolo" Binding="{Binding Titolo}"/>
                <DataGridTextColumn Header="Disco" Binding="{Binding Disco}"/>
                <DataGridTextColumn Header="Band" Binding="{Binding Band}"/>
                <DataGridTextColumn Header="Anno" Binding="{Binding Anno}"/>
                <DataGridTextColumn Header="Durata" Binding="{Binding Durata}"/>
            </DataGrid.Columns>
        </DataGrid>-->
    </DockPanel>
</Window>
